# This is the frontend side of the application. It provides access to the app user interface working with the backend of the app

In the project directory, you can run:
## `cd client`
## `npm install`
### `npm start`
